const { Client, ActivityType, Status, Events, GuildMember } = require("discord.js")

module.exports = {
    name: Events.GuildMemberAdd,
    /**
     * @param {Client} client
     * @param {GuildMember} member
    */
    async execute (member, client) {
        
    }
}